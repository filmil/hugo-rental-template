% SPDX-License-Identifier: Apache-2.0
%
% The brand mark: a simple house glyph, the favicon and small-size companion
% to brand-logo.tex. Replace it with your own mark -- keep it a single filled
% path (counters as even-odd HOLES, not white paint) so the same file works
% on light and dark surfaces. Ink colour is injected by //bazel:tikz.bzl.
\def\pgfsysdriver{pgfsys-dvisvgm.def}
\documentclass[tikz,border=0pt]{standalone}
\begin{document}
\begin{tikzpicture}[x=1mm, y=1mm]
  % dvisvgm sizes a DVI by its ink, so the canvas is pinned with a
  % zero-opacity stroke rather than an unstroked path.
  \draw[opacity=0,line width=0.01mm] (-0.2,-0.2) rectangle (10.2,10.2);
  % House outline (pentagon) with a door opening as an even-odd hole.
  \fill[even odd rule]
    (1.6,1.0) -- (8.4,1.0) -- (8.4,5.6) -- (5.0,8.7) -- (1.6,5.6) -- cycle
    (4.1,1.0) -- (5.9,1.0) -- (5.9,4.1) -- (4.1,4.1) -- cycle;
\end{tikzpicture}
\end{document}
